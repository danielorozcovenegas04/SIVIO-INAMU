@{
    Layout = null;
}
<script type="text/javascript">
    $(document).ready(function () {

        $('#btnDatosAdministrativos').click(function () {
            debugger;
            $.LoadingOverlay("show");
            $.ajax({
                type: "POST",
                url: '@Url.Action("CrearUsuaria_DatosAdministrativos", "Expediente")',
                async: true
            }).done(function (data) {

                $.LoadingOverlay("hide");
                $('#contendorFrames').html(data);
                $("#contenedorEspera").hide();
                $('#contendorFrames').fadeIn();


            }).fail(function (exception) {
                $("#txtModalTitulo").css("color", "red");
                $("#txtModalTitulo").text("Error de Sistema");
                $("#txtModalContenido").text("Fallo al cargar la pantalla, contacte a la persona administradora de SIPAMU");
                $("#contenedorEspera").hide();
                instMensajes.open();
                $.LoadingOverlay("hide");
            });
        });

        $('#btnDatosUsuaria').click(function () {
            debugger;
            $.LoadingOverlay("show");
            $.ajax({
                type: "POST",
                url: '@Url.Action("CrearUsuaria_DatosUsuaria", "Expediente")',
                async: true
            }).done(function (data) {

                $.LoadingOverlay("hide");
                $('#contendorFrames').html(data);
                $("#contenedorEspera").hide();
                $('#contendorFrames').fadeIn();


            }).fail(function (exception) {
                $("#txtModalTitulo").css("color", "red");
                $("#txtModalTitulo").text("Error de Sistema");
                $("#txtModalContenido").text("Fallo al cargar la pantalla, contacte a la persona administradora de SIPAMU");
                $("#contenedorEspera").hide();
                instMensajes.open();
                $.LoadingOverlay("hide");
            });
        });

        @*
        $('#btnDatosPerfil').click(function () {
            debugger;
            $.LoadingOverlay("show");
            $.ajax({
                type: "POST",
                url: '@Url.Action("CrearUsuaria_DatosPerfil", "Expediente")',
                async: true
            }).done(function (data) {

                $.LoadingOverlay("hide");
                $('#contendorFrames').html(data);
                $("#contenedorEspera").hide();
                $('#contendorFrames').fadeIn();


            }).fail(function (exception) {
                $("#txtModalTitulo").css("color", "red");
                $("#txtModalTitulo").text("Error de Sistema");
                $("#txtModalContenido").text("Fallo al cargar la pantalla, contacte a la persona administradora de SIPAMU");
                $("#contenedorEspera").hide();
                instMensajes.open();
                $.LoadingOverlay("hide");
            });
        });
        *@

        $('#btnHistoriaViolencia').click(function () {
            debugger;
            $.LoadingOverlay("show");
            $.ajax({
                type: "POST",
                url: '@Url.Action("CrearUsuaria_DatosAdministrativos", "Expediente")',
                async: true
            }).done(function (data) {

                $.LoadingOverlay("hide");
                $('#contendorFrames').html(data);
                $("#contenedorEspera").hide();
                $('#contendorFrames').fadeIn();


            }).fail(function (exception) {
                $("#txtModalTitulo").css("color", "red");
                $("#txtModalTitulo").text("Error de Sistema");
                $("#txtModalContenido").text("Fallo al cargar la pantalla, contacte a la persona administradora de SIPAMU");
                $("#contenedorEspera").hide();
                instMensajes.open();
                $.LoadingOverlay("hide");
            });
        });

        $('#btnAtencion').click(function () {
            debugger;
            $.LoadingOverlay("show");
            $.ajax({
                type: "POST",
                url: '@Url.Action("CrearUsuaria_DatosAdministrativos", "Expediente")',
                async: true
            }).done(function (data) {

                $.LoadingOverlay("hide");
                $('#contendorFrames').html(data);
                $("#contenedorEspera").hide();
                $('#contendorFrames').fadeIn();


            }).fail(function (exception) {
                $("#txtModalTitulo").css("color", "red");
                $("#txtModalTitulo").text("Error de Sistema");
                $("#txtModalContenido").text("Fallo al cargar la pantalla, contacte a la persona administradora de SIPAMU");
                $("#contenedorEspera").hide();
                instMensajes.open();
                $.LoadingOverlay("hide");
            });
        });

        $('#btnIntervencion').click(function () {
            debugger;
            $.LoadingOverlay("show");
            $.ajax({
                type: "POST",
                url: '@Url.Action("CrearUsuaria_DatosAdministrativos", "Expediente")',
                async: true
            }).done(function (data) {

                $.LoadingOverlay("hide");
                $('#contendorFrames').html(data);
                $("#contenedorEspera").hide();
                $('#contendorFrames').fadeIn();


            }).fail(function (exception) {
                $("#txtModalTitulo").css("color", "red");
                $("#txtModalTitulo").text("Error de Sistema");
                $("#txtModalContenido").text("Fallo al cargar la pantalla, contacte a la persona administradora de SIPAMU");
                $("#contenedorEspera").hide();
                instMensajes.open();
                $.LoadingOverlay("hide");
            });
        });

    });
</script>

<nav class="navbar navbar-default navbar-static-top borde" style="margin-top: 3px; padding-bottom: 5px !important; background-color: white !important">

    <div class="container" style="width: 80%; padding-left: 15px; padding-right: 15px;">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapse" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

        </div>

        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-left">

                <li class="nav">
                    <a href="#" id="btnDatosAdministrativos">Datos Administrativos</a>
                </li>

                <li class="nav">
                    <a href="#" id="btnDatosUsuaria">Datos de Usuaria</a>
                </li>

                <li class="nav">
                    <a href="#" id="btnDatosPerfil">Perfil de usuaria</a>
                </li>

                <li class="nav">
                    <a href="#" id="btnHistoriaViolencia">Historia de violencia</a>
                </li>

                <li class="nav">
                    <a href="#" id="btnAtencion">Atención</a>
                </li>

                <li class="nav">
                    <a href="#" id="btnIntervencion">Intervención</a>
                </li>
                
        </div>
    </div>

</nav>
